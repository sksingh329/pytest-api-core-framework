[pytest]

# ── Markers ──────────────────────────────────────────────────────────────────
markers =
    unit: unit tests that mock HTTP — no real network needed
    e2e_jsonplaceholder: e2e tests that hit jsonplaceholder.typicode.com

# ── Framework ────────────────────────────────────────────────────────────────
api_env            = dev
api_settings_module = config.settings
api_dotenv_file    = .env

# ── Report ───────────────────────────────────────────────────────────────────
addopts = --api-html-report=reports/{env}/report_{timestamp}.html -v
api_html_theme     = dark
api_html_title     = pytest-api-core Test Report
api_html_header    = pytest-api-core Framework Tests

# ── Logging ──────────────────────────────────────────────────────────────────
# api_log_level controls pytest_api_core logger (request/response output)
api_log_level      = DEBUG

# Console (live) log output
log_cli            = true
log_cli_level      = DEBUG
log_cli_format     = %(asctime)s [%(levelname)-8s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S

# Captured log output (shown in HTML report per test)
log_level          = DEBUG
log_format         = %(asctime)s [%(levelname)-8s] %(name)s: %(message)s
log_date_format    = %H:%M:%S
